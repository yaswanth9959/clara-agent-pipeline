# Demo transcripts (Pipeline A)

Put **plain-text transcript files** here. Each file must be `.txt`.

- **Filename = account ID** (without `.txt`).  
  Example: `bens_electrical.txt` → account_id = `bens_electrical`.
- Pipeline A will process every `.txt` file in this folder and create v1 memo + agent spec per account.
