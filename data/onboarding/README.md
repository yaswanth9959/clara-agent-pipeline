# Onboarding transcripts (Pipeline B)

Put **plain-text transcript files** here. Each file must be `.txt`.

- **Use the same account ID as the demo** for the same company.  
  Example: demo = `data/demo/bens_electrical.txt` → onboarding = `data/onboarding/bens_electrical.txt`.
- Pipeline B reads v1 from that account, applies onboarding updates, and writes v2 + changelog.
